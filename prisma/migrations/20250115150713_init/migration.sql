/*
  Warnings:

  - You are about to drop the column `description` on the `booktype` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `booktype` DROP COLUMN `description`;
